node_modules/
# CLI binaries are populated by scripts/build-zip.js — never committed
bin/btp
bin/cf
# build output
dist/
*.zip
*.sig
*.cert
# FAID app artifact channel — built by figaf-platform/spikes/app-manager-poc/build-artifacts.ps1
platform-artifacts/
# build record written by scripts/build-zip.js - a build artifact, never committed
bin/VERSIONS.json
